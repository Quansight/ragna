name: ragna-dev
channels:
  - conda-forge
dependencies:
  - python =3.9
  - redis-server
  - aiofiles
  - emoji
  - pip
  - pip:
    - jupyter
    - python-dotenv
    - boto3
    - pytest >=6
    - tomlkit
    - mypy
    - pre-commit
    # documentation
    
    # Temporary dependency for the new Panel Chat Interface.
    # TODO: remove this dependency once the new Panel Chat Interface is released,
    # and pin Panel's version
    - git+https://github.com/holoviz/panel@chat_components
    - mkdocs
    - mkdocs-material
    - mkdocstrings[python]
