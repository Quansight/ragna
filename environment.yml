name: ragna-dev
channels:
  - conda-forge
dependencies:
  - python =3.9
  # FIXME: This is not available on Windows
  #  Can we somehow mark this as "don't try to install on Windows?"
  - redis-server
  - pip
  - pip:
    - redis
    - jupyter
    - python-dotenv
    - boto3
    - pytest >=6
    - mypy
    - pre-commit
    # documentation
    - mkdocs
    - mkdocs-material
    - mkdocstrings[python]
