name: ragna-test
channels:
  - conda-forge
dependencies:
  - python =3.9
  # FIXME: This is not available on Windows. We need to document this as part of the
  #  dev installation
  #  - redis-server
  - pip
  - pip:
    - -r requirements.txt
